{layout parent.latte}

{block title}{$engine} | {include #parent}{/block}

{block content}
    {$content}
    {$xssContent}

    <hr />

    <script type="text/javascript">
        var userId = {$userId};
    </script>
    <p style="color: {$color};" title="{$attrib}">
        <a href="" onclick="return !confirm({$message});">{$desc}</a>
    </p>
    <!-- Executed in: {$time} s -->
    <hr />
    <h2>Using plugins</h2>
        
    escaped: {$pluginText|changeText}<br />
    nofilter: {!$pluginText|changeText}
    
{/block}
